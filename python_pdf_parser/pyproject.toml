[project]
name = "python-parser"
version = "0.1.0"
description = "Flexible PDF parser for extracting table data"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pdfplumber>=0.11.0",
    "pandas>=2.0.0",
]
